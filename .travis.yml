language: nodejs

node_js:
  - "12"

before_script:
  - npm install

jobs:
  include:
    - stage: lint
      script: npm run lint

    - stage: build
      script: npm build

    - stage: test
      scripts:
        - npm test
      after_success:
        - codecov
